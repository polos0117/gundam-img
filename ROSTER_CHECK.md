# 기체 로스터 완비 여부 추적

방식: `kr.gundam-official.com/series/[코드]/mecha` 목록을 받아 우리 목록과 대조해
빠진 것만 넣는다. 예전에는 자바스크립트 렌더링 페이지라 손으로 옮겨 적었는데,
Firecrawl 이 렌더링해 주므로 이제는 받아서 자동으로 대조한다.

받아 둔 목록은 `official/mecha/`, 대조는 `build-official.py`, 카드 생성은
`build-roster.py` 가 맡는다. 크레딧을 들여 받은 자료라 캐시가 아니라 저장소에 둔다.

---

## 완료 (대조 끝)

2026-09-05 부터는 공식 사이트 목록을 Firecrawl 로 직접 받아 대조한다.
`official/mecha/<코드>.json` 에 받아 둔 것을 `build-official.py` 가 읽어
저장소와 맞춰 본다. 손으로 옮겨 적을 일이 없어져서 28 개 시리즈를 한 번에
끝냈다. 지금 대조 결과는 추가 후보 0 — 공식 목록 기준으로 빠진 기체가 없다.

| 시리즈 | 코드 | 방식 | 결과 |
|---|---|---|---|
| 28 개 전 시리즈 | — | **공식 사이트 자동 대조** | 기체 516 · 함선 23 추가 (272→788, 43→66) |

이전에 손으로 끝낸 것들(NT, GQuuuuuuX, 섬광의 하사웨이, 건담 00 극장판,
SEED ASTRAY 스타게이저)은 자동 대조에서도 추가 후보 0 으로 확인됐다.

## 대기

없다. 새 시리즈가 공식 사이트에 올라오면 `official-slugs.json` 에 슬러그를
넣고 그 시리즈의 `mecha` 페이지를 받아 `build-official.py` 를 돌리면 된다.

## 자료가 어디 있나

카드 자료의 원본은 **`data/*.json`** 이다. 예전에는 `play.html` 안에 자바스크립트
배열로 박혀 있었는데, 카드 하나의 정보가 `MECH`·`WEIGHT`·`LORE`·`TAG_COLOR`·
`TAG_ROLE`·`GUNDAM_EXTRA` 여섯 군데로 흩어져 이름으로만 이어져 있었다. 그래서
표기가 한 글자 갈리면 같은 기체가 두 장이 됐다. 지금은 카드 하나가 레코드 하나다.

| 파일 | 내용 |
|---|---|
| `data/mech.json` | 기체 788. 능력치·세력·시리즈에 형식번호(`models`)·G 제네레이션 id(`gge`)·레어도·지형 적성·분류 태그, 드래프트 비중·태그·한 줄 소개까지 한 레코드에 |
| `data/pilot.json` | 파일럿 210. `gge`·레어도·분류 태그 포함 |
| `data/ship.json` · `crew.json` | 모함 66 · 지휘관 33 |
| `data/bond.json` | 전용기 연대 327 쌍 |
| `data/combo.json` | 연대·혈연·악연 164 |
| `data/img.json` | 초상 파일 목록 |
| `data/series.json` | 시리즈 이름·정규화 단위·진영 색 |

읽고 쓰는 길은 `roster.py` 하나다. 스크립트마다 정규식으로 뜯어 읽던 것을
없앴다. 게임 규칙 코드는 아직 자리번호로 읽으므로(`m[7]` 은 계열) `roster.rows()`
가 예전 배열 꼴로 되돌려 준다.

`play.html` 은 실행할 때 `data/` 를 `fetch` 로 읽는다. 정적 호스팅 그대로지만
`file://` 로 열면 브라우저가 막으므로 `python3 -m http.server` 로 띄워야 한다.
`dex.html` 은 만들 때 자료를 박아 넣으므로 파일 하나로 열린다.

### 형식번호

`models` 는 두 곳에서 온다 — 공식 사이트 상세(`official/pilot/relations.json`)와
소샤지 G 제네레이션 API(`id-map.json`). 엇갈리면 공식을 따르고, 공식이 여러 카드를
한 항목에 몰아 적은 자리만 `data-overrides.json` 에 사람이 적어 둔다. 788 기 중
502 기(64%)가 갖고 있다. 나머지는 어느 쪽에도 안 실린 단역·양산기다.

형식번호가 있으면 이름이 갈려도 같은 기체인지 드러난다. 실제로 이걸로
`V건담`/`빅토리 건담`(둘 다 LM312V04)을 잡아냈다.

### 바깥에서 가져온 칸

`build-data.py` 가 소샤지 API 캐시에서 카드로 옮겨 붙인다. 게임 규칙은 아직
쓰지 않는다 — 자리부터 만들어 두고 무엇이 쓸 만한지 보는 단계다.

| 칸 | 뜻 | 채워진 정도 |
|---|---|---|
| `rarity` | G 제네레이션 레어도 1~5 | 기체 466 · 인물 133 |
| `terrain` | 지형 적성. 우주·대기권·지상·수상·수중, **1 부적합 · 2 보통 · 3 적합** | 기체 466 |
| `tags` | 분류 태그 | 기체 460 · 인물 131 |
| `large` | 두 칸을 차지하는 대형기 | 기체 14 |

태그는 100 종 가운데 골라 남긴다. 시리즈·진영 태그는 `series` 와 `factions` 로
이미 갖고 있어 겹치고, 돌파력·일격필살 같은 것은 G 제네레이션 전투 안에서만
뜻이 있어 여기로 가져와도 가리키는 데가 없다. 남기는 목록은
`build-data.TAG_MECH` / `TAG_PILOT` 에 있다.

지형 적성은 원작의 물리가 아니라 **게임의 밸런스 표**다. 건탱크가 우주 3 이고
윙 건담 제로가 수중 3 이다. 경향은 맞지만 낱개로는 믿을 값이 아니다.

### 대조 결과

`python3 build-data.py --report` 가 새 태그와 손으로 적어 둔 값이 어긋나는 곳을
짚어 준다. 고치지는 않는다 — 소샤지 태그는 게임이 편하려고 거칠게 나눈 것이고
(색이 흰·빨강·검정·금 넷뿐이다), 반대로 손으로 적은 쪽이 틀린 적도 있다.

지금 나오는 것: 색 어긋남 27 · 색 빈칸 138 · 역할 어긋남 1 · 역할 빈칸 76 ·
계열 어긋남 10 · 감응 등급 0 인데 게임은 감응자로 보는 인물 6.

계열 어긋남에는 우리 자료 안에서 앞뒤가 안 맞는 데도 섞여 있다. `ZZ건담` 은
사이코뮤인데 `강화형 ZZ건담` 과 `풀 아머 ZZ건담` 은 시작기로 적혀 있다.

## 사용법 (다음 세션에서)

1. `official-slugs.json` 에 시리즈 코드와 공식 사이트 슬러그를 넣는다
2. Firecrawl 로 `series/<슬러그>/mecha` 를 받아 `official/mecha/<코드>.json` 에 넣는다
   (`{"세력": ["이름", ...]}` 꼴. 이 환경에서 공식 사이트는 직접 접근이 막혀 있다)
3. `python3 build-official.py --report` 로 대조하고, `python3 build-roster.py` 로
   카드를 만든다. 카드는 `data/` 에 들어간다
4. `python3 build-idmap.py` 로 G 제네레이션 id 를 다시 받고,
   `python3 build-data.py` 로 형식번호와 id 를 카드에 붙인다
5. `python3 build-dex.py play.html` 로 도감을, `python3 build-prompt.py` 로
   툴킷(prompt.html)의 기체 사본을 다시 만든다. 둘 다 `data/` 를 원본으로 삼는다
   (`build-dex.py` 는 `play.html` 에서 표시용 선언 셋만 더 가져온다)

## 참고

- "하로" 같은 마스코트 로봇, 차량, 시설은 기체 로스터에서 제외 (`gundam_match.EXCLUDE_NAMES`)
- 함선은 제외가 아니라 SHIP 으로 돌린다 (`gundam_match.SHIP_NAMES` / `SHIP_SUFFIX`)
- 장비/사양 배리에이션도 이제는 다 넣는다. 빼는 대신 드래프트 등장 비중을 낮춘다
  (`WEIGHT` 0.15). 도감은 완비되고 팩에 잡히는 체감은 그대로다
- 스탯 4개는 공식 수치가 없어서 임의 배정이다. `build-roster.py` 가 특성별 평균
  (양산 65/67/65/66, 사이코뮤 90/88/82/78, 핵동력 92/89/83/91 …)에서 이름 해시만큼
  흔들어 매긴다. 같은 이름은 늘 같은 값이 나온다. 개별 조정은 `roster-overrides.json`
- soshage 수치를 옮겨 쓰려 했으나 212 기로 맞춰 보니 축끼리 상관이 0.27~0.54 로 낮고
  대응도 어긋나 쓰지 않았다. 게임 밸런스용 수치와 원작 인상 기준 수치는 다른 것을 잰다
- 같은 기체가 공식 표기와 저장소 표기로 갈려 두 장이 된 것 10 장을 걷어냈다.
  건담 NT-1 Alex↔건담 NT-1, 건담 칼리번↔건담 캘리번, 윙 건담 0↔윙 건담 제로 EW,
  건담 DX↔더블 엑스, 유니콘 건담 3호기 페넥스↔페넥스,
  리본즈 건담/리본즈 캐논↔리본즈 건담, 고트라탄↔고틀라탄, 리그 샷코↔리그 샤코, 샷코↔샤코,
  빅토리 건담↔V건담.
  손질해 둔 쪽(그림·설명·인연이 붙은 쪽)을 남기고 반입분을 지운 뒤
  `roster-overrides.json` 의 `skip` 과 `build-bond.ALIAS_MECH` 에 넣어 다시 안 생기게 했다.
  이름으로 찾을 때는 `gundam_match.norm` 으로 편 다음 자모로 풀어(NFD) 유사도를 잰다.
  글자 단위로만 재면 칼리번/캘리번 같은 한 글자 차이를 놓친다. 이름이 아예 다른
  V건담/빅토리 건담 은 그래도 못 잡는다 — 그건 형식번호가 잡았다
- 총 기체 수: 788, 함선 66 (2026-09-05 기준)
- 드래프트 비중: 기존 272 기가 1.0, 새 조연·에이스기 113 기가 0.4, 양산·배리에이션 403 기가 0.15.
  9 장 팩 기준으로 기존 272 기가 팩의 71% 를 차지한다
